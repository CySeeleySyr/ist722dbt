version: 2

models:
  - name: obt_sales
    description: The sales fact table combined with all of its related dimensions into a single table. Transaction fact table with one row per line item ordered, excluding product data.
